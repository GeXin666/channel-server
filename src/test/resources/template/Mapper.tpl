package ${mapperPackage};

import ${domainPackage}.${sn};
import ${domainPackage}.${sn}Example;
import com.framework.core.mybatis.base.BaseMapper;

public interface ${sn}Mapper extends BaseMapper<${sn}, ${sn}Example> {

}